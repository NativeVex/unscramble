'use strict';

var _toConsumableArray = require('babel-runtime/helpers/to-consumable-array')['default'];

var _interopRequireWildcard = require('babel-runtime/helpers/interop-require-wildcard')['default'];

Object.defineProperty(exports, '__esModule', {
  value: true
});

var _actionsShortcuts = require('../actions/shortcuts');

var types = _interopRequireWildcard(_actionsShortcuts);

exports['default'] = function (state, action) {
  if (state === undefined) state = [];

  switch (action.type) {
    case types.ADD_SHORTCUTS:
      return [].concat(_toConsumableArray(state), _toConsumableArray(action.shortcuts));
    case types.REMOVE_SHORTCUTS:
      return state.filter(function (shortcut) {
        return !action.shortcuts.find(function (actionShortcut) {
          return shortcut.key === actionShortcut.key;
        });
      });
    default:
      return state;
  }
};

module.exports = exports['default'];