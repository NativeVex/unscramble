'use strict';

var _extends = require('babel-runtime/helpers/extends')['default'];

Object.defineProperty(exports, '__esModule', {
  value: true
});

var _actionsServers = require('../actions/servers');

var initialState = {
  isSaving: false,
  server: null,
  error: null
};

exports['default'] = function (state, action) {
  if (state === undefined) state = initialState;

  switch (action.type) {
    case _actionsServers.SAVE_SERVER_PREPARE:
      return _extends({}, initialState, { server: action.server || null });
    case _actionsServers.SAVE_SERVER_REQUEST:
      return _extends({}, initialState, { isSaving: true, server: action.server });
    case _actionsServers.SAVE_SERVER_SUCCESS:
      return _extends({}, state, { isSaving: false });
    case _actionsServers.SAVE_SERVER_FAILURE:
      return _extends({}, state, { isSaving: false, error: action.error.validationErrors });
    default:
      return state;
  }
};

module.exports = exports['default'];